# Rust build output
/target/

# Per-developer briefs / scratch notes (not part of the public docs)
/ai-docs/

# Personal Claude Code settings (hooks, tool permissions)
.claude/settings.local.json

# cargo-fuzz build output and crash artifacts (corpus is intentionally tracked)
crates/*/fuzz/target/
crates/*/fuzz/artifacts/
